---
version: 2

deployment:
  name: Deploy Test
  source-plan: BAM-BAM

release-naming:
  next-version-name: release-${bamboo.release-number}
  applies-to-branches: true
  auto-increment: true

environments:
  - Test  

Test:
  tasks:
    - clean